declare namespace common {
  interface articleListItem {
    title: string,
    createTime: number,
    intro: string,
    fileName: string,
  }
  
  interface headerItem {
    showName: string;
    name: string;
  }
}